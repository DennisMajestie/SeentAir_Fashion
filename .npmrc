# Render sets NODE_ENV=production, which makes npm default to --omit=dev.
# Our build (nest build) and boot-time migrations (ts-node/typeorm) need
# devDependencies present, so force them in regardless of NODE_ENV.
include=dev