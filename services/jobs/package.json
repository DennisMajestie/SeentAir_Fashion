{
  "name": "@seentair/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "Seentair background workers (BullMQ) — stub until notifications/report generation land (Phase 5+)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
